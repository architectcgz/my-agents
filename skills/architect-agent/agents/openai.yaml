interface:
  display_name: "Architect Agent"
  short_description: "Analyze runtime, delivery, boundaries, and safe change surfaces"
  default_prompt: "Use $architect-agent from the target repository root to analyze module boundaries, production runtime and delivery gates, aggregation candidates, risks, and the smallest safe change path."
